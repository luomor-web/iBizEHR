import { Http,Util } from '@/utils';
import GenerateFJLogicBase from './generate-fj-logic-base';

/**
 * 生成附件信息
 *
 * @export
 * @class GenerateFJLogic
 */
export default class GenerateFJLogic extends GenerateFJLogicBase{

    /**
     * Creates an instance of  GenerateFJLogic
     * 
     * @param {*} [opts={}]
     * @memberof  GenerateFJLogic
     */
    constructor(opts: any = {}) {
        super(opts);
    }

}