import { Http,Util } from '@/utils';
import KBLogicBase from './kb-logic-base';

/**
 * 开班
 *
 * @export
 * @class KBLogic
 */
export default class KBLogic extends KBLogicBase{

    /**
     * Creates an instance of  KBLogic
     * 
     * @param {*} [opts={}]
     * @memberof  KBLogic
     */
    constructor(opts: any = {}) {
        super(opts);
    }

}