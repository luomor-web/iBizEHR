import ContractSignORGUIServiceBase from './contract-sign-org-ui-service-base';

/**
 * 签约主体单位UI服务对象
 *
 * @export
 * @class ContractSignORGUIService
 */
export default class ContractSignORGUIService extends ContractSignORGUIServiceBase {

    /**
     * Creates an instance of  ContractSignORGUIService.
     * 
     * @param {*} [opts={}]
     * @memberof  ContractSignORGUIService
     */
    constructor(opts: any = {}) {
        super(opts);
    }

}