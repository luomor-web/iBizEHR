import { Http,Util } from '@/utils';
import SocAreaServiceBase from './soc-area-service-base';


/**
 * 参保地服务对象
 *
 * @export
 * @class SocAreaService
 * @extends {SocAreaServiceBase}
 */
export default class SocAreaService extends SocAreaServiceBase {

    /**
     * Creates an instance of  SocAreaService.
     * 
     * @param {*} [opts={}]
     * @memberof  SocAreaService
     */
    constructor(opts: any = {}) {
        super(opts);
    }


}