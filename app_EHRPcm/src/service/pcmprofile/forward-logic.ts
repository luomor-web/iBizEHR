import { Http,Util } from '@/utils';
import ForwardLogicBase from './forward-logic-base';

/**
 * 上报
 *
 * @export
 * @class ForwardLogic
 */
export default class ForwardLogic extends ForwardLogicBase{

    /**
     * Creates an instance of  ForwardLogic
     * 
     * @param {*} [opts={}]
     * @memberof  ForwardLogic
     */
    constructor(opts: any = {}) {
        super(opts);
    }

}