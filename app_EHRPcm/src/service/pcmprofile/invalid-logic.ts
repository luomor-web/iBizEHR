import { Http,Util } from '@/utils';
import InvalidLogicBase from './invalid-logic-base';

/**
 * 失效
 *
 * @export
 * @class InvalidLogic
 */
export default class InvalidLogic extends InvalidLogicBase{

    /**
     * Creates an instance of  InvalidLogic
     * 
     * @param {*} [opts={}]
     * @memberof  InvalidLogic
     */
    constructor(opts: any = {}) {
        super(opts);
    }

}