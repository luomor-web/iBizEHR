import { Http,Util } from '@/utils';
import ORMPostLibServiceBase from './ormpost-lib-service-base';


/**
 * 岗位库服务对象
 *
 * @export
 * @class ORMPostLibService
 * @extends {ORMPostLibServiceBase}
 */
export default class ORMPostLibService extends ORMPostLibServiceBase {

    /**
     * Creates an instance of  ORMPostLibService.
     * 
     * @param {*} [opts={}]
     * @memberof  ORMPostLibService
     */
    constructor(opts: any = {}) {
        super(opts);
    }


}